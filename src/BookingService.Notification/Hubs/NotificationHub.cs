using Microsoft.AspNetCore.SignalR;

namespace BookingService.Notification.Hubs;

public class NotificationHub : Hub
{
    // No methods are required since the server only sends messages to the clients.
}
